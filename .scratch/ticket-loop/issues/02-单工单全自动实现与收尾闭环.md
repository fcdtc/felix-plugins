# 02: 单工单全自动实现与收尾闭环

**What to build:** 用户可以启动只包含一张 Task Ticket 的 Loop Run。系统创建持久账本，为工单启动全新 Claude Code Session，以严格的 `/implement @工单路径` prompt 实施；实现提交后精确恢复同一 Session 执行固定 Closeout；只有实现提交、工单状态、验收清单、完成证据及干净工作区全部通过时，运行才标记完成。

**Blocked by:** 01 / 工单计划与依赖预检

**Status:** done (2026-09-15, 1d02fa4)

- [x] 提供单工单可用的 `start`、`step` 和 `status` 命令，并通过结构化 JSON 暴露状态与允许动作。
- [x] 启动时冻结仓库、分支、起始 HEAD、Manifest、实现命令和 Session ID。
- [x] 新工单使用预生成 UUID 的全新持久 Session，首条 prompt 严格为 `/<resolved-implement-command> @<ticket-absolute-path>`，不追加文字。
- [x] 实现阶段结束后校验至少一个新 commit、起始 HEAD 的祖先关系、干净工作区和无未完成 Git 操作。
- [x] 接受一个或多个实现 commit，不要求压缩为单 commit。
- [x] 实现门禁通过后使用精确 Session ID 恢复原 Session，并发送冻结的 Closeout prompt。
- [x] Closeout 核验全部 checklist、done 状态和唯一的 Completion evidence。
- [x] Completion evidence 包含非空的 Implementation、Typecheck、Tests 和 Review；`Not applicable` 必须带原因。
- [x] Closeout 有剩余修改时使用 `chore(ticket-loop): close 02` 提交；没有修改时不创建空 commit。
- [x] 实现 commit 已包含部分或全部工单收尾信息时，不改写历史，只补齐缺失内容。
- [x] 全部门禁通过后运行标记 completed，工作区保持干净，且不 push、不创建 PR、不切换分支。
- [x] 使用 fake Claude executable 从 CLI 边界验证完整实现与收尾闭环，不调用真实模型。
